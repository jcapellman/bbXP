<Page
    x:Class="bbxp.UWP.Views.ContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:bbxp.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:bbxp.UWP.ViewModels"
    xmlns:userControls="using:bbxp.UWP.UserControls"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewModels:ContentViewModel />
    </Page.DataContext>

    <Grid Style="{StaticResource GridPageBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Style="{StaticResource PostTitleGrid}" Margin="10,5,10,5">
            <TextBlock Text="{Binding Content.Title}" Style="{StaticResource PageHeaderText}" />
        </Grid>

        <userControls:HTMLRenderView Grid.Row="1" ContentBody="{Binding Content.Body}" />
    </Grid>
</Page>