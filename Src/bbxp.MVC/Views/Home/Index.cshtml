@using bbxp.PCL.Transports.Posts
@model List<bbxp.PCL.Transports.Posts.PostResponseItem>

@{
    ViewData["Title"] = "Home";
}

<h2 class="PageTitle">latest posts</h2>

@foreach (var post in Model) {
    @Html.Partial("_PostPartial", post)
}