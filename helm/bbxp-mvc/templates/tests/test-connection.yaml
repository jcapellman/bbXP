apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bbxp-mvc.fullname" . }}-test-connection"
  labels:
    {{- include "bbxp-mvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bbxp-mvc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
